[project]
name = "dbt-codespace-demo"
version = "0.1.0"
description = "Example dbt project for the Codespace workshop (Postgres + DuckDB)"
requires-python = ">=3.10,<3.13"
dependencies = [
    "dbt-core>=1.9,<1.10",
    "dbt-postgres>=1.9,<1.10",
    "dbt-duckdb>=1.9,<1.10",
    "dbt-metabase>=1.7,<2.0",
]
