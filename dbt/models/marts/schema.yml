version: 2

models:
  - name: customer_summary
    description: "Pro Kunde Anzahl Bestellungen, Umsatz und erstes/letztes Bestelldatum."
    columns:
      - name: customer_id
        tests: [unique, not_null]
