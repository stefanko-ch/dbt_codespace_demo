version: 2

models:
  - name: customer_summary
    description: "Per-customer order count, total revenue, and first/last order dates."
    columns:
      - name: customer_id
        tests: [unique, not_null]
